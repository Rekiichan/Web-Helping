@*@model IEnumerable<BulkyBook.Models.Product>
@{
    ViewData["Title"] = "Index";
}

<partial name="_Notification" />*@
<div class="container p-3">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-primary fw-bold"> Danh sách sản phẩm</h2>
        </div>
        <div class="col-6  text-end">
            <a asp-controller="Product" asp-action="Upsert" class="btn btn-primary">
                <i class="bi bi-plus-circle"></i> &nbsp; Create New Product
            </a>
        </div>
    </div>
    <br /><br />
    <table id="tblData" class="table table-hover table-striped border-dark bg-white" style="width:100%">
        <thead>
            <tr class="table-dark">
                <th width="30%">
                    <a class="nav-link text-white">Tên</a>
                </th>
                <th width="15%">
                    <a class="nav-link text-white">Mã</a>
                </th>
                <th width="5%">
                    <a class="nav-link text-white">Giá</a>
                </th>
                <th width="10%">
                    <a class="nav-link text-white">Công ty</a>
                </th>
                <th width="15%">
                    <a class="nav-link text-white">Màu</a>
                </th>
                <th>
                    <a class="nav-link text-white">ID</a>
                </th>
            </tr>
        </thead>
    </table>
</div>

@section Scripts{
    <script src="~/js/product.js"></script>
}